spring:
  profiles: container
  jpa:
    database: MYSQL
    hibernate:
      ddl-auto: validate

  datasource:
    url: jdbc:mysql://mysql:3306/db_hibernate
    username: hibernate
    password: admin
    driver-class-name: com.mysql.jdbc.Driver